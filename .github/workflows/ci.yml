name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - name: Unit tests
        run: npm test
      - name: Anonymity lint
        run: npm run lint:anon
      - name: Build smoke test
        run: |
          npm run build:cli
          node dist-cli/cli/index.js build -c triage.config.example.yml
          test -f dist/triage.html
          if grep -q 'src="http' dist/triage.html; then
            echo "✗ built HTML references an external script — single-file invariant broken"
            exit 1
          fi
          echo "✓ dist/triage.html is self-contained"
      - name: Pages artifact in sync
        run: |
          npm run build:pages
          if ! git diff --quiet -- docs/index.html; then
            echo "✗ docs/index.html is stale — run 'npm run build:pages' and commit the result"
            git diff -- docs/index.html | head -40
            exit 1
          fi
          echo "✓ docs/index.html matches a fresh generic build"
