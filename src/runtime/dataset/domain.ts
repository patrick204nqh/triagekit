// src/runtime/dataset/domain.ts — transitional shim; removed in a later task.
export type { Class, DomainId, Domain } from "./taxonomy";
export { listDomains, getDomain, domainOf, classOf } from "./taxonomy";
