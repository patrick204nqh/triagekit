# AGENTS.md

Read automatically by Claude Code and by OpenCode. Per <https://agents.md>.

**Read `CONTEXT.md` at every session start** — it has everything: project identity,
conventions, design system, test commands (human-authored `<GOAL>` block) and active
work state (AI-maintained). Do not proceed with any task until you've read CONTEXT.md.
